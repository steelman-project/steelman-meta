You are continuing work in a repository that may have improved its own workflow since the last session.

Before doing substantive work:
1. Re-read the current checked-in source of truth from disk.
2. Prefer the latest repository state over any prior session assumptions.
3. Treat previous plans as provisional unless they still match the current files.
4. Untracked files or unstaged changes in the working tree are work-in-progress from a previously interrupted run, not noise. Inspect them, decide whether they belong to the current phase, and either fold them in or discard them deliberately — do not ignore them.

Read and follow, in this order, if present:
- .claude/CLAUDE.md
- CLAUDE.md
- docs/META_SPEC.md
- docs/META_PHASES.md
- docs/QUALITY_GATES.md
- docs/CAPABILITY_MANIFEST.md
- capabilities/project-capabilities.yaml
- docs/ARCHITECTURE.md
- docs/PHASES.md
- docs/PROD_REQUIREMENTS.md
- docs/USAGE_PATTERNS.md

Operating rules:
- Start from the earliest unapproved phase or meta-phase
- Work in audit-first mode
- Prefer approval over rewrite when existing implementation satisfies the phase
- Prefer minimal, backward-compatible fixes when a phase is partially complete
- Do not skip quality gates
- Use strategy-planner for material design choices
- Use architecture-red-team for architecture, autonomy, packaging, workflow, or control-loop changes
- Before starting a new phase, review prior-phase code for overlap with the current task (drift detection)
- Before starting a phase that builds on prior user-visible or end-to-end work, run a verification sprint (full test suite + primary workflow exercise) per QUALITY_GATES.md
- Verify that builder deliverables include tests before sending to code-reviewer
- Require test-driven development: tests written before or alongside implementation, not after
- Require code-reviewer approval before phase signoff
- Require qa-playwright for user-visible functionality
- Require skill validation and packaging for skill-producing phases
- In unattended mode, do not ask the user for confirmation merely to proceed with planning, red-team review, or internal phase decomposition
- In unattended mode, do not ask for permission to write files — bypassPermissions is active, write directly to any file including .claude/agents/ and artifacts/
- If new work is discovered and it is self-resolvable, update docs and phase structure as needed and continue or stop cleanly with a phase-update artifact
- If genuine external input is required, write `artifacts/phase-blocked.json` and stop
- CRITICAL: Never exit without writing one of the four expected artifacts (phase-approval.json, phase-update.json, phase-blocked.json, project-complete.json) — the outer loop depends on finding one
- Do not proceed past a completed phase without writing `artifacts/phase-approval.json`
- If the project is complete, write `artifacts/project-complete.json`
- Do not continue into the next phase until the repository has been committed externally

How to determine what is already approved:
- Check `artifacts/phase-approval.json` — it records the most recently approved phase
- Check `git log` for prior phase approval commits
- Phases up to and including the one in `phase-approval.json` are approved
- Start work on the next phase after that

First task:
1. If `artifacts/phase-verify-failed.json` exists, the previous iteration's phase-approval or phase-update commit was blocked by the pre-commit verification gate. Treat fixing that failure as the highest-priority work for this iteration — read the artifact's `command`, `log_tail`, and `attempts` fields, then fix the underlying problem before doing any new phase work. Re-write whichever signal artifact you wrote previously (`phase-approval.json` or `phase-update.json`, preserving the same phase identity) when the fix is in place; the wrapper will retry the commit.
2. Read `artifacts/phase-approval.json` to find the last approved phase
3. Identify the next unapproved phase from docs/META_PHASES.md or docs/PHASES.md
4. List the minimum work needed for that phase
5. Execute only that phase
6. Stop after approval, phase update, blocker, or completion